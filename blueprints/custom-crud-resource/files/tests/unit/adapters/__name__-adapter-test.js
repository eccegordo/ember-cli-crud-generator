import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('adapter:<%= typeSingular %>', '<%= capSingular %> Adapter', {
  // Specify the other units that are required for this test.
  // needs: ['adapter:foo']
});

// Replace this with your real tests.
test('it exists', function() {
  var adapter = this.subject();
  ok(adapter);
});
